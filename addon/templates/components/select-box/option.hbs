{{#if hasBlock}}
  {{~yield (hash
    index=index
    value=internalValue
    isPending=isPending
    isRejected=isRejected
    isFulfilled=isFulfilled
    isSettled=isSettled
    isSelected=isSelected
    isDisabled=isDisabled
  )~}}
{{else}}
  {{~yield~}}
{{/if}}