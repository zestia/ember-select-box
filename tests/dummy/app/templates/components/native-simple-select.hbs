<div class="simple-select-display-label">
  {{this.displayLabel}}
</div>
<NativeSelectBox
  @value={{@value}}
  @onSelect={{@onSelect}}
  @onFocus={{action "focused"}}
  @onBlur={{action "blurred"}} as |sb|>
  {{yield sb}}
</NativeSelectBox>