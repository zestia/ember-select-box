<SelectBox
  @classNamePrefix="search-select"
  @value={{@value}}
  @searchSlowTime={{500}}
  @onSelect={{@onSelect}}
  @onSearch={{@onSearch}}
  @onSearched={{action "searched"}}
  @onPressEscape={{action "close"}}
  @onClickOutside={{action "close"}} as |sb|>
  <sb.Input />
  {{#if sb.isSearching}}
    <sb.Option>
      Please wait...
      {{if sb.isSlowSearch "(hold tight!)"}}
    </sb.Option>
  {{else if sb.isOpen}}
    <sb.Options>
      {{yield sb this.lastQuery}}
    </sb.Options>
  {{/if}}
</SelectBox>